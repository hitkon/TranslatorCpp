#include <iostream>
// My Comment
int main()
{
    double sum = 0;
    double middle = a + j * h;
    while (a < b){
        a++;
        b = b - 1;
    }
    while(a > 3)
    {
        if (a > 5)
            b = 7 - b * 4;
    }
    //qwer
    for(int i = 0; i < n; i=i+1){}
    if (sum < middle)
        middle = 5;
    else if(sum == middle)
        middle = "str";
    else{
        middle = 3;
    }
    std::cout << "Hello World!\n";
}